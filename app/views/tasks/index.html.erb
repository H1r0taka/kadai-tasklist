<!-- Controller から渡されたデータを一覧表示させる -->
<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %>
  <% end %>
</ul>

<%= link_to '新規タスクの登録', new_task_path %>